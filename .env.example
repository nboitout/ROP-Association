# Lead-capture form: POSTs from /api/lead are forwarded here.
# Create a Google Apps Script (Tools → Script Editor on a Sheet),
# implement doPost(e) to append JSON.parse(e.postData.contents) to a row,
# Deploy → New deployment → type "Web app", access "Anyone".
# Paste the deployment URL here (and in Vercel project env vars).
LEAD_WEBHOOK_URL=
